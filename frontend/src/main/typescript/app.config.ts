import { ExpoConfig } from '@expo/config-types';

const config: ExpoConfig = {
    name: "portfolio",
    slug: "portfolio",
    // entryPoint: "io/github/jervnorsk/index.tsk"
};
export default config;
